# ==========================================
# JavaScript 実践課題（初学者向け / Node.jsで実行）
# ==========================================

## 前提
- `index.js` を作って、以下で実行します

```bash
node index.js
```

---

# ------------------------------------------
# 【課題0】準備：データを用意する
# ------------------------------------------

## 目的
配列・オブジェクトを扱う練習の土台を作る。

## 手順
1. `index.js` に以下をコピペして保存する

```js
const users = [
  { id: 1, name: "Taro", coin: 100, level: 5, lastLoginDaysAgo: 1, items: ["potion"] },
  { id: 2, name: "Hanako", coin: 300, level: 12, lastLoginDaysAgo: 10, items: ["sword", "potion"] },
  { id: 3, name: "Saburo", coin: 50, level: 2, lastLoginDaysAgo: 0, items: [] },
  { id: 4, name: "Yuki", coin: 1200, level: 20, lastLoginDaysAgo: 40, items: ["shield"] },
];

console.log("users:", users);
```

2. 実行して配列が表示されることを確認する

---

# ------------------------------------------
# 【課題1】合計コインを計算する（for / reduce）
# ------------------------------------------

## 目的
ループ or reduceで合計を出す練習。

## 要件
- `合計コイン: xxxx` の形式で出力する
- 方法は `for` でも `reduce` でもOK

## 期待出力例
```
合計コイン: 1650
```

---

# ------------------------------------------
# 【課題2】条件で抽出する（filter）
# ------------------------------------------

## 目的
filterで「条件に合うユーザーだけ」を取り出す練習。

## 要件
- coinが100以上のユーザーだけ抽出して表示する
- 表示は `console.log(filteredUsers)` でOK

---

# ------------------------------------------
# 【課題3】データを変換する（map）
# ------------------------------------------

## 目的
mapで「配列の中身を変換」する練習。

## 要件
- 全ユーザーにログインボーナスとして `+100 coin` した新しい配列を作る
- 元の `users` は変更しない（重要）
- 出力で「元のusers」と「新しいusers」を比較できるように表示する

## ヒント
- `map` を使う
- `...user`（スプレッド構文）を使うと楽

---

# ------------------------------------------
# 【課題4】検索して更新する関数を作る（find / update）
# ------------------------------------------

## 目的
関数化＋findで対象を見つける練習。

## 仕様
- `rewardUser(users, userId, amount)` を作る
- 指定ユーザーの coin を amount 分だけ増やした「新しい配列」を返す
- 対象ユーザーがいなければ `throw new Error("User not found")` する

## テスト例
```js
const updated = rewardUser(users, 2, 500);
console.log(updated);
```

---

# ------------------------------------------
# 【課題5】集計する（reduce / object）
# ------------------------------------------

## 目的
reduceで集計する練習（運営バックエンドで超よくやる）。

## 要件
- 「レベル帯ごとの人数」を集計してオブジェクトで出す
- レベル帯の定義：
  - 1〜5   => "low"
  - 6〜15  => "mid"
  - 16以上 => "high"

## 期待出力例
```js
{ low: 2, mid: 1, high: 1 }
```

---

# ------------------------------------------
# 【課題6】配列の中の配列を扱う（items集計）
# ------------------------------------------

## 目的
ネストしたデータ（配列の中に配列）が扱えるようになる。

## 要件
- 全ユーザーが持っているアイテムの出現回数を集計する
- 例：potionが2回、swordが1回…みたいに

## 期待出力例
```js
{ potion: 2, sword: 1, shield: 1 }
```

---

# ------------------------------------------
# 【課題7】非同期の入口（Promise / async-await）
# ------------------------------------------

## 目的
バックエンドで必須の async/await の入口に慣れる。

## 仕様
- `sleep(ms)` を作る（msミリ秒待つPromise）
- `async function main()` を作って以下を順番に表示する
  1. "start"
  2. 500ms待つ
  3. "after 500ms"
  4. さらに500ms待つ
  5. "done"

## ヒント実装
```js
const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
```

---

# ------------------------------------------
# 【ボーナス課題】ミニ運営っぽい問題
# ------------------------------------------

## 要件
- `lastLoginDaysAgo` が 30日以上のユーザーを「休眠ユーザー」として抽出
- 休眠ユーザーにだけ coin を 0 にする（新しい配列で）
- 休眠ユーザーの人数と、更新後の配列を表示

---

# ------------------------------------------
# 提出（自分用チェック）
# ------------------------------------------

- [ ] 課題1ができた
- [ ] 課題2ができた
- [ ] 課題3が「元データを変更せず」にできた
- [ ] 課題4で存在しないIDでエラーが出せた
- [ ] 課題5,6でreduce集計できた
- [ ] 課題7でasync/awaitの流れを理解した
